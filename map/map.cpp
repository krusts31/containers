#include "map.hpp"

/*
iterator	map::begin()
{
	return iterator(t);
}

iterator	map::end()
{
	return iterator(t + );
}

unsigned int	Map::size(void)
{
	unsigned int	size;

	;
	return (size);
}
bool ft::map::empty() const
{
	if (_val = NULL)
		return (true);
	return (false);
}
*/
